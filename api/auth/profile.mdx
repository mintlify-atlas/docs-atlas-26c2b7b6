---
title: 'GET /api/auth/profile'
description: 'Get the current authenticated user profile'
---

## Authentication

<Warning>
  This endpoint requires authentication. Include the JWT token in an HTTP-only cookie named `jwt`.
</Warning>

The request must include a valid JWT token that was obtained during login or registration. The token is automatically sent via cookies if you're using the same client.

## Request

This endpoint does not accept any request parameters or body.

## Response

<ResponseField name="_id" type="string">
  The unique identifier for the user.
</ResponseField>

<ResponseField name="username" type="string">
  The username of the user.
</ResponseField>

<ResponseField name="email" type="string">
  The email address of the user.
</ResponseField>

<ResponseField name="phone" type="string">
  The phone number of the user.
</ResponseField>

<ResponseField name="isAdmin" type="boolean">
  Whether the user has admin privileges.
</ResponseField>

<ResponseField name="createdAt" type="string">
  The ISO 8601 timestamp when the user account was created.
</ResponseField>

<ResponseField name="avatar" type="object">
  The user's avatar information.
  
  <Expandable title="properties">
    <ResponseField name="url" type="string">
      The URL of the user's avatar image.
    </ResponseField>
    
    <ResponseField name="publicId" type="string">
      The public ID for the avatar (if using cloud storage).
    </ResponseField>
  </Expandable>
</ResponseField>

<ResponseField name="listings" type="number">
  The count of listings associated with this user (virtual field).
</ResponseField>

<RequestExample>

```bash cURL
curl -X GET https://api.boundry.com/api/auth/profile \
  -H "Content-Type: application/json" \
  -b "jwt=your_jwt_token_here"
```

```javascript JavaScript
const response = await fetch('https://api.boundry.com/api/auth/profile', {
  method: 'GET',
  headers: {
    'Content-Type': 'application/json',
  },
  credentials: 'include' // Important for cookies
});

const data = await response.json();
```

</RequestExample>

<ResponseExample>

```json Success Response (200)
{
  "_id": "60d5ec49f1b2c72b8c8e4f3a",
  "username": "johndoe",
  "email": "john.doe@example.com",
  "phone": "+12345678901",
  "isAdmin": false,
  "createdAt": "2021-06-25T14:30:15.000Z",
  "avatar": {
    "url": "https://avatar.iran.liara.run/public/boy?username=johndoe",
    "publicId": ""
  },
  "listings": 5
}
```

```json Error - Token Not Found (401)
{
  "message": "Token Authentication Failed :: Token not found"
}
```

```json Error - Token Invalid (401)
{
  "message": "Token Authentication Failed :: Token not verified"
}
```

```json Error - User Not Found (404)
{
  "message": "User not found"
}
```

</ResponseExample>
